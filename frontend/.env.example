# This is the ONE setting that connects frontend to backend.
# Vite only exposes env vars prefixed with VITE_ to the browser.
# Must match the PORT your backend is actually running on.
VITE_API_URL=http://localhost:5001/api
